#version: '3.11.7'
services:
  api:
    build: .
    container_name: fastapi-management
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
    env_file:
      - .env
  db:
    image: mysql
    #    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
